

const SelfMessage = (props) => {
    return (
        <div>
            <div className="self-message">
                {props.message}
            </div>
        </div>

    )
}

export default SelfMessage;